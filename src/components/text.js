const addToCart=()=>{
    setCart(
      ...cart,
      {
        id:i,
        name:name,
        quantity:count,
        price:price * count,
      },
      console.log(count)
    );
  }

  cart.length==0?setText("unavailable")
  <AddCart id={i}/>

  .btncart{
    margin-left: 400px;
    color:black;
  }



.btn{
  
}